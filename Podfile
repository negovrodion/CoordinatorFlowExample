# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CoordinatorFlowTestProject' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'CoordinatorFlow'

  # Pods for CoordinatorFlowTestProject

  target 'CoordinatorFlowTestProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CoordinatorFlowTestProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
